# The production path only. vLLM, torch and their CUDA pairing come from the base image and are
# deliberately absent here — pinning them again would let this file and the image disagree.

# The serverless harness: `runpod.serverless.start`.
runpod>=1.7,<2

# The HTTP client the passthrough forwards with. httpx rather than the `openai` SDK on purpose:
# this worker must be able to forward a route the SDK has never heard of — that is the entire
# point of it — and an SDK's typed methods are a closed set by construction. It is already in the
# image as a transitive dependency of vLLM's own server; naming it here is what makes that
# explicit rather than accidental.
httpx>=0.27,<1
