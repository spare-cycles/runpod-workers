__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Bench artefacts: the corpus is real voice notes and their transcripts — private conversation, and
# never something to commit — and the results file is a per-machine measurement log.
corpus/
bench-results.jsonl

# The local check venv (see README). Disposable; recreated by the `uv venv` line there.
.venv/
